## UI/UX & Blocos — Achados

| # | Arquivo:Linha | Severidade | Problema visual | Print mental (o que o usuário vê) | Correção |
|---|---|---|---|---|---|
| 1 | `components/MediaUploader.tsx:154` | **Alta** | Botão remover arquivo usa `opacity-0 group-hover:opacity-100` — invisível em touch | Mobile: usuário arrasta foto, vê lista de arquivos mas não há botão X para remover; toca no card e nada acontece | Trocar para `opacity-100 sm:opacity-0 sm:group-hover:opacity-100` ou `opacity-60 sm:opacity-0` + sempre visível em touch |
| 2 | `components/Calendar/MonthView.tsx:62-75` | **Alta** | Grid fixo `grid-cols-7` + `min-h-[140px]` + `aspect-[4/5]` sem breakpoint mobile | 375px: 7 colunas de ~51px, cards thumbnail 40x32px espremidos, badge + hora sobrepostos, leitura impossível; scroll vertical infinito | `grid-cols-7` só em `md:` — mobile: lista vertical por dia ou `grid-cols-2` empilhado; `min-h-[90px] md:min-h-[140px]` |
| 3 | `components/Calendar/WeekView.tsx:68-75` | **Alta** | WeekView também `grid-cols-7` + `min-h-[600px]` + `aspect-[9/16]` sem mobile | 375px: 7 colunas minúsculas, cards 30px de largura, usuário faz pinch, overflow horizontal fantasma | Mobile: `flex-col` ou horizontal swipe com snap; `hidden md:grid` + alternativa lista |
| 4 | `components/Calendar/CalendarHeader.tsx:14-38` | **Alta** | Header `px-6 py-4` + `text-[34px]` + dois botões sem `flex-wrap` | 375px: título "Calendário" 34px + botão "Agendar post" estouram largura, quebra linha feia ou corta botão; filtro sobreposto | `flex-wrap gap-2`, título `text-[24px] sm:text-[34px]`, botão com label curto `+ Agendar` em mobile, `px-4 sm:px-6` |
| 5 | `components/TabBar.tsx:76-98` | **Alta** | 6 itens nav + FAB = 7 alvos em 60px de altura, `flex-1` + `text-[9px]` truncate | iPhone SE: labels "Configurações"/"Biblioteca" truncam "Config…", badges `-right-1.5` cortados na borda, toque erra alvo | Reduzir para 4 tabs + "Mais" (overflow menu); ou `gap-0.5` e esconder label em 375px (`hidden xs:block` com ícone only) |
| 6 | `components/AppShell.tsx:42-43` | **Média** | `pb-[83px] md:pb-0` + `main p-4 md:p-8 max-w-5xl` vs Calendar `overflow-hidden px-4 pb-10` e `position:fixed TabBar` | Mobile: TabBar cobre último card do calendário; `pb-10` não compensa safe-area iOS (notch), conteúdo atrás da barra | Usar `pb-[calc(83px+env(safe-area-inset-bottom))]` e `pb-safe` no main; remover `overflow-hidden` do calendar container |
| 7 | `components/Sidebar.tsx:57` | **Baixa** | Sidebar `hidden md:flex w-64 sticky h-screen` + `bg-ios-card dark:bg-[#111]` | Desktop 1440px: sidebar fixa 256px ok, mas dark `#111` diverge de `--ios-card #1C1C1E` — linha vertical com tonalidade diferente | Unificar `dark:bg-[var(--ios-card)]` ou `dark:bg-[#1C1C1E]`; considerar `xl:w-72` fluido |
| 8 | `components/IOSComponents.tsx:6` + `app/planners/page.tsx:701,734,849` | **Média** | Raio de borda inconsistente: `ios-card 12px` vs modais `rounded-2xl (16px)` vs `rounded-3xl (24px)` | Usuário percebe 3 arredondamentos diferentes percorrendo app; modal de delete parece outro design system | Tokenizar: `rounded-xl (12px) card`, `rounded-2xl (16px) modal` apenas; remover 3xl |
| 9 | `components/IOSButton.tsx:12` | **Média** | Base `w-full` obriga `!py-2 !px-4` override em todo uso inline | Canais/Planners: botão "Adicionar canal" estica 100% se esquecer override; inconsistência padding 12px vs 10px | Base sem `w-full`; criar variantes `block` vs `inline`; padding padronizar `py-2.5 px-4` |
| 10 | `components/ContentLibrary.tsx: ~385` (loading) | **Média** | Loading apenas spinner `animate-spin` centralizado, sem skeleton | Usuário vê flash branco → spinner → grid repentino = layout shift 400px; perceived performance ruim | Skeleton grid: 6 cards `animate-pulse bg-ios-gray-5 aspect-square rounded-xl` preservando altura |
| 11 | `app/analytics/page.tsx:244` `app/planners/page.tsx:541` | **Média** | Heatmap `overflow-x-auto min-w-max` + BarChart `h-32 viewBox 100x40` sem indicador de scroll | Mobile: heatmap exige scroll horizontal mas sem sombra/arrow; usuário acha que são só 4 colunas | Adicionar `scrollbar-thin` + `after: fade` shadow + hint "← arraste →" ; chart `w-full aspect-[2/1]` |
| 12 | `app/planners/page.tsx:555-623` | **Média** | Card planner `flex items-center gap-3` sem `flex-wrap`, título sem `truncate`, stats `flex flex-wrap gap-x-4` | 375px: nome longo + badge "active" empurram botões de ação para fora da tela; `Next: em 2h 13min` quebra 2 linhas | `min-w-0` + `truncate` no h4, `flex-wrap` no container principal, ações `shrink-0` com `gap-1` |
| 13 | `app/channels/page.tsx:378` (card Instagram) | **Média** | Card Instagram usa `opacity-0 group-hover:opacity-100` vs YouTube `opacity-100 md:opacity-0` | Mobile Instagram: botões editar/teste/delete invisíveis (precisa hover), usuário não descobre ação | Padronizar `opacity-100 md:opacity-0 md:group-hover:opacity-100` em ambos |
| 14 | `components/ContentLibrary.tsx:2121-2140` | **Baixa** | Table headers escondem progressivamente `hidden sm` / `md` / `lg` / `xl` mas celas desalinham com header sortable | Tablet 768px: vê colunas Name+Type mas header click area de "Size" ainda ocupando espaço colapsado | Usar `table-fixed` + `colgroup`; ou colapsar em card list abaixo `md` ao invés de esconder TH |
| 15 | `app/settings/page.tsx:481` + `623` | **Baixa** | Grid `grid-cols-1 sm:grid-cols-2 gap-4` ok mas YouTube health `grid-cols-3 gap-2` com `text-[13px] truncate` | 375px: 3 colunas de 100px, versão/truncado sem tooltip, números colados | `grid-cols-1 sm:grid-cols-3` mobile stack; adicionar `title` já existe mas aumentar `gap-3` |
| 16 | `app/youtube/comments/page.tsx:278,354` | **Baixa** | Seletor de canal `flex gap-2 overflow-x-auto -mx-1 px-1` sem `snap` nem `scrollbar-hide` consistente | Usuário não percebe scroll; em desktop com 6 canais há overflow mas sem seta | `snap-x snap-mandatory` + botão chevron scroll; `scrollbar-hide` padronizar |
| 17 | `app/page.tsx:394` + `app/analytics/page.tsx:966` | **Baixa** | Chips de filtro `flex gap-2 overflow-x-auto scrollbar-hide` com `whitespace-nowrap` | Mobile: chips cortados na direita sem fade; usuário não sabe que há mais filtros | `mask-image: linear-gradient(to right, transparent, black 16px, black calc(100%-16px), transparent)` |
| 18 | `app/globals.css: ios-card / ios-blur` | **Média** | Sombras inconsistentes: `shadow-sm border /50` em card, `shadow-xl` toast, `shadow-2xl` modal, header `ring-1 ring-black/5` vs `shadow-sm` | Elevação sem escala (0,1,2,3) parece aleatória; cards flutuam igual a modais | Escala: card `shadow-sm`, modal `shadow-xl`, toast `shadow-lg` — remover `shadow-2xl` |
| 19 | `components/Calendar/MonthView.tsx:102` `WeekView.tsx:146` | **Média** | Imagens sem `width`/`height` nem `aspect` reservado no placeholder `No Media` vs `aspect-[4/5]` no container com imagem | CLS: quando imagem carrega, altura do dia salta 20px; placeholder "Video" sem mesma altura | Sempre reservar `aspect-[4/5]` mesmo no fallback div; add `width/height` attr ou `aspect-square` |
| 20 | `app/analytics/page.tsx:735,920` `app/planners/page.tsx:490` `app/channels/page.tsx:310` | **Média** | Empty state bom mas sem ilustração consistente; loading sem retry em Analytics LocalDashboard | Usuário com erro de rede vê spinner infinito; botão "Tentar novamente" só em Insights, não no dashboard local | Adicionar `error` + `retry` em todo fetch com card `AlertTriangle + Tentar novamente` (já existe em channels) |
| 21 | `app/new/page.tsx:391,466` | **Média** | Preview `aspect-[9/16] max-w-[200px]` centralizado `mx-auto` vs grid `grid-cols-5 gap-2` community images `aspect-square` | Mobile: 5 colunas em 375px = 65px por thumb, sem gap responsivo; título cortado sem `line-clamp` | `grid-cols-3 sm:grid-cols-5`, thumbs `rounded-lg` consistente com uploader |
| 22 | `components/IOSComponents.tsx: IOSRow / IOSGroup` | **Baixa** | Tipografia mista: `text-[17px]`, `text-[15px]`, `text-[13px]`, `text-[11px]` sem escala; `tracking-wide` só em header group | Percepção de hierarquia fraca; "Library" 20px vs "Calendário" 34px desbalanceado | Definir scale 34/20/17/13/11 e aplicar uniforme; ContentLibrary "Library" → `text-[34px]` ou i18n "Biblioteca" |
| 23 | `components/ChannelModal.tsx` + `MoveContentModal` | **Média** | Modais sem `max-h-[dvh]` e `overflow-y-auto` limitado; em 375x667 teclado cobre inputs | Settings/ChannelModal: usuário não consegue scrollar form quando teclado abre (viewport trava) | `max-h-[85dvh] overflow-y-auto` + `pb-[env(keyboard-inset-height)]` |
| 24 | `app/page.tsx:257` `components/Calendar/*.tsx` | **Baixa** | Ícones desalinhados: Sidebar 20px stroke2, TabBar 22px stroke2, CalendarHeader Chevron 24px, Planner card Play 22px fill | Peso visual difere; Chevron 24px domina header | Padronizar 20px navegação, 16px ações, 24px apenas hero; `strokeWidth 1.75` consistente |

**Resumo executivo:** 24 achados — 5 Alta (bloqueiam mobile), 10 Média (polimento), 9 Baixa. Focos: (1) Calendar precisa variante mobile lista; (2) touch targets invisíveis (MediaUploader, Channel card); (3) skeleton vs spinner para evitar layout shift; (4) tokenizar radius/shadow/typography.
